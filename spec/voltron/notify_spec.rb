require 'spec_helper'

describe Voltron::Notify do
  it 'has a version number' do
    expect(Voltron::Notify::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
